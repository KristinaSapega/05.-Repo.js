//1
/* Напишите функцию, которая возвращает меньшее из 2 чисел.
 Например, из двух чисел — 8 и 4 — функция должна вернуть 4. 
Если передадим 6 и 6, то функция должна вернуть 6. */

const puzzle = (a, b) => {
  if (a < b) {
    return a;
  } else {
    return b;
  }
};

console.log(puzzle(8, 4));

//2
/* Напишите функцию, которая принимает число, 
а возвращает строку 'Число четное', если число четное, 
'Число нечетное' — если нечетное.*/

const task = (n) => {
    if (n % 2 == 0) {
        return "Число четное";
    } else {
        return "Число нечетное";
    }
  };
  console.log(task (5));
  console.log(task (4));

  //3
  /* 3.1. Напишите функцию, которая принимает параметром число 
  и выводит в консоль квадрат этого числа.
    const exercise = (n) => {
    console.log(n ** 2);
  }
  exercise (7);

  3.2. Напишите функцию, которая принимает параметром число 
  и возвращает квадрат этого числа. */

  const exercise2 = (n) => {
    return n ** 2;
  }
  exercise2 (7);
  
/* Разница между двумя приведенными функциями заключается в том, 
как они обрабатывают результат. 
Обе функции принимают число в качестве аргумента и возводят его в квадрат, 
но первая функция (exercise) выводит результат в консоль,
 а вторая функция (exercise2) возвращает результат в виде значения,
которое можно использовать в других частях программы.
Таким образом, если вызвать функцию exercise(7), она выведет результат в консоль, 
то есть число 49.
Но это значение нельзя использовать в других частях программы, 
так как оно не возвращается из функции.
Если вызвать функцию exercise2(7), она вернет значение 49, 
которое можно использовать в других частях программы. 
Например, можно записать результат в переменную и использовать ее дальше в программе. */
